<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <link rel="icon" type="image/x-icon" href="favicon.ico" />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Google+Sans:wght@400;500;700&family=Google+Sans+Text:wght@400;500&display=swap"
            rel="stylesheet"
        />
        <meta name="generator" content={Astro.generator} />
        <meta
            name="description"
            content="A simple, private budgeting app. One-time purchase. No subscriptions, no ads, no tracking."
        />
        <meta
            property="og:title"
            content="Kakeibo — Know where your money goes"
        />
        <meta
            property="og:description"
            content="A simple, private budgeting app. One-time purchase. No subscriptions, no ads, no tracking."
        />
        <meta property="og:type" content="website" />
        <meta name="twitter:card" content="summary_large_image" />
        <meta
            name="twitter:title"
            content="Kakeibo — Know where your money goes"
        />
        <meta
            name="twitter:description"
            content="A simple, private budgeting app. One-time purchase. No subscriptions, no ads, no tracking."
        />
        <title>Kakeibo — Know where your money goes</title>
    </head>
    <body>
        <slot />
    </body>
</html>

<style>
    :root {
        /* Colors from theme/tokens.ts */
        --background: #141515;
        --surface: #3e3e3e;
        --surface-hover: #4a4a4a;
        --text: #ffffff;
        --subtext: #8f8f8f;
        --red: #ef476f;
        --blue: #118ab2;
        --green: #06d6a0;
        --yellow: #ffd166;
        --border: rgba(255, 255, 255, 0.06);

        /* Spacing */
        --spacing-xs: 4px;
        --spacing-sm: 8px;
        --spacing-md: 16px;
        --spacing-lg: 24px;
        --spacing-xl: 32px;
        --spacing-2xl: 48px;

        /* Border radius */
        --radius-sm: 4px;
        --radius-md: 8px;
        --radius-lg: 16px;
        --radius-xl: 24px;
        --radius-pill: 999px;
    }

    html {
        scroll-behavior: smooth;
    }

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        font-family:
            "Google Sans Text",
            "Google Sans",
            -apple-system,
            BlinkMacSystemFont,
            sans-serif;
        background-color: var(--background);
        color: var(--text);
        line-height: 1.6;
        min-height: 100vh;
    }
</style>
